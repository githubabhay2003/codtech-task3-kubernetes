apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  # ClusterIP is the default and exposes the service only within the cluster
  type: ClusterIP
